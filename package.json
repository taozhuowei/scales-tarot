{
  "name": "scales-tarot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npm run start --prefix app",
    "build": "npm run build:h5 --prefix app"
  },
  "workspaces": [
    "app",
    "server"
  ],
  "dependencies": {
    "express": "^4.18.2"
  }
}
